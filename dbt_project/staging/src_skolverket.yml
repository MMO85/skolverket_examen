version: 2

sources:
  - name: skolverket
    # Use the active target schema (e.g. staging_data in your profiles.yml)
    # This avoids hardcoding "main" and prevents team-specific mismatches.
    schema: "{{ target.schema }}"
    tables:
      - name: raw_data
      - name: np_ak9
